<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
            @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');

        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
        }
        body * {
            box-sizing: border-box;
        }
        .login-principal {
            width: 100vw;
            height: 100vh;
            background: #323232;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .rigth_login {
            width: 90vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .card-login{
            width: 95%;
            height: 80%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            padding: 30px 35px;
            background: #404040;
            box-shadow: 0px 10px 40px #00000056;
            border-radius: 70px;
        }
        .card-login > h1 {
            color: #ff7300;
            font-weight: 800;
            margin: 0;
            border-radius: 10px;
        }

        .card-login > h2 {
            color: #ffffff;
            font-weight: 800;
            margin: 0;
            border-radius: 10px;
        }
        

        .textfield {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            justify-content: center;
            margin: 10px 0px;
        }
        .textfield > input {
            width: 100%;
            border: none;
            padding: 15px;
            background: #afafaf;
            color: #ffffff;
            font-size: 12pt;
            box-shadow: 0px 10px 40px #00000056;
            outline: none;
            box-sizing: border-box;
        }
        .textfield > label {
            color: #ffffff;
            margin-bottom: 10px;
        }
        .textfield > input::placeholder {
            color: #5b5b5b;
        }
        .btn-login {
            width: 100%;
            padding: 16px 0px;
            margin: 25px;
            border: none;
            outline: none;
            font-weight: 600;
            font-size: 12pt;
            color: #ffffff;
            background: #ff7300;
            cursor: pointer;
        }
        @media only screen and (max-width: 950px) {
            .card-login {
                width: 90%;
            }
        }
        @media only screen and (max-width: 600px) {
           .rigth_login {
            width: 100%;
            height: auto;
           }
        }
        .card-login {
            width: 100%;
        }
        .registro > a {
            color: #ff7300;
            font-weight: 800;
            margin: 0;
        }


    </style>
</head>
<body>
    <div class="login-principal">
        <form method="post">
        <div class="rigth_login">
            <div class="card-login" >
                <h1>Seja Bem-Vindo!</h1>
                <h2>Faça abaixo o Login:</h2>
                <div class="textfield" met>
                    <label for="usuario">Qual é o seu Usuário?</label>
                    <input type="text" name="login" placeholder="Usuário">
                </div>
                <div class="textfield">
                    <label for="senha">Qual é a sua Senha?</label>
                    <input type="password" name="senha" placeholder="Senha">
                </div>
                <button class="btn-login">Clique aqui para fazer Login</button>
                <div class="registro">
                    <a href="./cadastro.ejs" target="_blank">Cadastre-se</a>
                    </div>
            </div>
        </div>
    </form>
    </div>
</body>
</html>